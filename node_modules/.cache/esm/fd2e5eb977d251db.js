let Router,DOMAIN,Profile,User,uploader,userAuth;_77d‍.x([["default",()=>_77d‍.o]]);_77d‍.w("express",[["Router",["Router"],function(v){Router=v}]]);_77d‍.w("../constants",[["DOMAIN",["DOMAIN"],function(v){DOMAIN=v}]]);_77d‍.w("../models",[["Profile",["Profile"],function(v){Profile=v}],["User",["User"],function(v){User=v}]]);_77d‍.w("../middlewares/uploader",[["default",["uploader"],function(v){uploader=v}]]);_77d‍.w("../middlewares/auth-guard",[["userAuth",["userAuth"],function(v){userAuth=v}]]);





const router = Router();

/**
 * @description To create profile of the authenticated User
 * @type POST <multipart-form> request
 * @api /profiles/api/create-profile
 * @access Private
 */
router.post(
  "/api/create-profile",
  userAuth,
  uploader.single("avatar"),
  async (req, res) => {
    try {
      let { body, file, user } = req;
      var f = file.path.split('\\');
      var fileName = f[f.length-1];
      _77d‍.g.console.log(fileName)
      let profile = new Profile({
        social: body,
        account: user._id,
        avatar: DOMAIN+fileName,
      });
      await profile.save();
      return res.status(201).json({
        success: true,
        messgae: "Profile created successfully.",
      });
    } catch (err) {
      return res.status(400).json({
        success: false,
        messgae: "Unable to create your profile.",
      });
    }
  }
);

/**
 * @description To Get the authenticated user's profile
 * @api /profiles/api/my-profile
 * @access Private
 * @type GET
 */
router.get("/api/my-profile", userAuth, async (req, res) => {
  try {
    let profile = await Profile.findOne({ account: req.user._id }).populate(
      "account",
      "name email username"
    );
    _77d‍.g.console.log(req.user)
    if (!profile) {
      return res.status(404).json({
        success: false,
        message: "Your profile is not available.",
      });
    }
    return res.status(200).json({
      success: true,
      profile,
    });
  } catch (err) {
    return res.status(400).json({
      success: false,
      message: "Unable to get the profile.",
    });
  }
});

/**
 * @description To update autheticated user's profile
 * @type PUT <multipart-form> request
 * @api /profiles/api/update-profile
 * @access Private
 */
router.put(
  "/api/update-profile",
  userAuth,
  uploader.single("avatar"),
  async (req, res) => {
    try {
      let { body, file, user } = req;
      var f = file.path.split('\\');
      var fileName = f[f.length-1];
      let profile = await Profile.findOneAndUpdate(
        { account: user._id },
        { social: body, avatar: DOMAIN+fileName },
        { new: true }
      );
      return res.status(200).json({
        success: true,
        message: "Your profile is now updated",
        profile,
      });
    } catch (err) {
      return res.status(400).json({
        success: false,
        message: "Unable to get the profile.",
      });
    }
  }
);

/**
 * @description To get user's profile with the username
 * @api /profiles/api/update-profile
 * @access Public
 * @type GET
 */
router.get("/api/profile-user/:username", async (req, res) => {
  try {
    let { username } = req.params;
    let user = await User.findOne({ username });
    if (!user) {
      return res.status(404).json({
        success: false,
        message: "User not found.",
      });
    }
    let profile = await Profile.findOne({ account: user._id });
    return res.status(200).json({
      profile: {
        ...profile.toObject(),
        account: user.getUserInfo(),
      },
      success: true,
    });
  } catch (err) {
    return res.status(400).json({
      success: false,
      message: "Something went wrong.",
    });
  }
});

_77d‍.d(router);