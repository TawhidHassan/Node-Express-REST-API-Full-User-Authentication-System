let check;_600‍.x([["RegisterValidations",()=>RegisterValidations],["AuthenticateValidations",()=>AuthenticateValidations]]);_600‍.w("express-validator",[["check",["check"],function(v){check=v}]]);

const name = check("name", "Name is required.").not().isEmpty();
const username = check("username", "Username is required.").not().isEmpty();
const email = check("email", "Please provide a valid email address").isEmail();
const password = check(
  "password",
  "Password is required of minimum length of 6."
).isLength({
  min: 6,
});

       const RegisterValidations = [password, name, username, email];
       const AuthenticateValidations = [username, password];